# 二维装箱：轮廓束搜索伪代码

目标：在 \(H\times W\) 容器中装填矩形件。状态是「压缩轮廓 \(A\) + 已装集合 \(S\)」。
不跑可采纳 A\*；按已装件数分层，层内只保留前 \(k\) 个状态。
空穴只用于选落点，状态之间用 \(g + h_{\text{上界}}\) 比较。

---

## 符号

```
件 i:     (h_i, w_i)，可旋转则还有 (w_i, h_i)
容器:     高 H、宽 W，从左、从底装填
轮廓 A:   分段 skyline，每段 s = (y0, y1, rem)
          表示行 [y0, y1) 右侧剩余宽度为 rem
          不变式: 段按 y 拼接覆盖 [0, H)，相邻段 rem 不同
状态 u:   (A, S, g)
          S = 已装件集合
          g = 已装体积（或真实价值）
剩余 R:   全部件 \ S
```

死空间与破碎度只看「对剩余件已经不可能再放」的残段，不把普通凹槽当空穴。

---

## 主流程

```
function BeamPack(items, H, W, k, pos_keep, λ, μ, γ):
    start ← State(
        A   = [Seg(0, H, W)],   // 整箱空
        S   = ∅,
        g   = 0
    )
    beam ← [start]
    best ← start                // 始终记下 g 最大且合法的状态

    // 按已装件数分层，避免 3 件好轮廓和 10 件烂装填直接比 g
    for layer ← 0 to |items|-1:
        cand ← ∅                // 本层后继

        for u in beam:
            succs ← Expand(u, items, H, W, pos_keep, λ, μ)
            for v in succs:
                cand.append(v)
                if v.g > best.g:
                    best ← v

        if cand 为空:
            break

        cand ← DominateSameS(cand)          // 同 S 轮廓支配
        beam ← SelectTopK(cand, k, items, γ)

    // 可选：从 best 或最终 beam 里再局部搜索排列
    return best 以及 beam 中 g 最大者
```

---

## 展开一个状态

每件只生成少量角点落点，用位置分选最好的几个，再生成后继。

```
function Expand(u, items, H, W, pos_keep, λ, μ):
    R ← items \ u.S
    if R 为空:
        return ∅

    dead0 ← DeadSpace(u.A, R)
    frag0 ← Fragments(u.A)
    succs ← ∅

    for i in R:
        for (h, w) in Orientations(i):      // 不旋转则只有一种
            slots ← CandidateSlots(u.A, h, w, W)
            scored ← ∅

            for slot in slots:
                A2 ← Place(u.A, slot, h, w)
                R2 ← R \ {i}
                dead1 ← DeadSpace(A2, R2)
                frag1 ← Fragments(A2)

                // 位置分：只在「同一件的不同落点」之间比
                pos_score ← volume(i)
                            - λ * max(0, dead1 - dead0)
                            - μ * (frag1 - frag0)

                scored.append((pos_score, A2, slot))

            scored.sort_desc by pos_score
            for (pos_score, A2, slot) in scored[1 .. pos_keep]:
                succs.append(State(
                    A = A2,
                    S = u.S ∪ {i},
                    g = u.g + volume(i)     // 或价值 value(i)
                ))

    return succs
```

`pos_keep` 建议 1～3。不要对每一行都试一次。

---

## 候选落点：最低缺口 / 角点

不要扫全部 \(y\in[0,H)\)。只在轮廓拐角处尝试把件的底边贴上去。

```
function CandidateSlots(A, h, w, W):
    slots ← ∅
    segs ← A 的段列表

    // 每个段顶当「水平线」，尝试把件放在这段左侧贴齐的位置
    for s in segs:
        y ← s.y0
        if y + h > H:
            continue

        // 件覆盖的行 [y, y+h) 上，右侧剩余宽度的最小值必须 ≥ w
        rem_min ← min{ t.rem | t 与 [y, y+h) 相交 }
        if rem_min < w:
            continue

        // 左齐：该矩形贴在这些行当前装填的最右沿
        // x_left = W - rem_min ，件占用 [x_left, x_left+w)
        slots.append(Slot(y=y, w=w, h=h))

        // 可选：同一水平线上，只保留「最低 y」和「使新轮廓段数最少」的
        // 不要把所有 y 都留下

    return unique(slots)
```

放置后把覆盖段的 `rem` 减去 `w`，再合并相邻同 `rem` 的段。

```
function Place(A, slot, h, w):
    A2 ← copy(A)
    把 A2 中与 [slot.y, slot.y+h) 相交的段，按 y 切开
    对切开后完全落在 [slot.y, slot.y+h) 内的段:
        seg.rem ← seg.rem - w
        断言 seg.rem ≥ 0
    Merge 相邻 rem 相同的段
    return A2
```

---

## 真死空间、破碎度、状态分

```
function min_edge(R):
    if R 为空: return +∞
    return min{ min(h_j, w_j) | j in R }    // 允许旋转则取可放的最小边

function DeadSpace(A, R):
    m ← min_edge(R)
    dead ← 0
    for s in A:
        // 残段在「宽」或「高」上已经塞不进任何剩余件
        if s.rem < m or (s.y1 - s.y0) < m:
            dead ← dead + s.rem * (s.y1 - s.y0)
        // 更严：连续若干段拼成的最大空矩形，若仍放不进 R 中任何一件，
        // 把该矩形面积计入 dead（实现时可对 skyline 求最大空矩形）
    return dead

function Fragments(A):
    return |A|                              // 段数；越碎越大
    // 可加: Var({s.rem}) 或 1 / 最大空矩形面积

function H_upper(u, items):
    R ← items \ u.S
    area_left ← sum{ s.rem * (s.y1 - s.y0) | s in u.A } - DeadSpace(u.A, R)
    vol_left  ← sum{ volume(j) | j in R }
    return min(vol_left, max(0, area_left))

function LargeItemPenalty(u, items):
    // 剩余大件若已几乎无落点，现在不放后面更没地
    R ← items \ u.S
    pen ← 0
    for j in R:
        if volume(j) < 大件阈值:          // 例如体积前 30% 才算大件
            continue
        nslot ← 0
        for (h, w) in Orientations(j):
            nslot ← nslot + |CandidateSlots(u.A, h, w, W)|
        if nslot == 0:
            pen ← pen + volume(j)         // 已经装不下：这是硬伤
        else if nslot == 1:
            pen ← pen + 0.5 * volume(j)   // 唯一槽，fail-first
    return pen

function StateScore(u, items, γ):
    // 状态分：比较不同后继用这个；不要加累计空穴
    return u.g + H_upper(u, items) - γ * LargeItemPenalty(u, items)
```

\(\gamma\) 取 \(0.5\sim 1\) 量级即可。空穴不要加进 `StateScore`。

---

## 同 S 支配

剩余空间处处不少于对方，且已装同一批件，则对方可丢。

```
function Dominates(u, v):
    if u.S ≠ v.S:
        return false
    // 把两份轮廓对齐到同一套 y 切分后比较 rem
    for 每一行 y:
        if Remaining(u.A, y) < Remaining(v.A, y):
            return false
    return true                             // u 的可行域覆盖 v

function DominateSameS(cand):
    按 S 分组
    keep ← ∅
    for 每一组 G:
        删掉被组内其它状态 Dominates 的
        keep.append 剩余
    return keep
```

实现上同 S 状态不会太多：可把 \(A\) 做成哈希，只对哈希碰撞或同桶的做逐段比较。

---

## 层内 top-k + 多样性

```
function SelectTopK(cand, k, items, γ):
    for u in cand:
        u.f ← StateScore(u, items, γ)

    // 轮廓近亲不要占满 k 个名额
    buckets ← 按 BucketKey(u.A) 分桶
    // BucketKey 示例: 段数、最低 rem、最高 rem、dead 的粗量化

    picked ← ∅
    while |picked| < k 且 还有未取状态:
        for b in buckets 按 round-robin:
            从 b 中取尚未选取的 f 最大者
            picked.append 该状态
            if |picked| == k:
                break

    return picked
```

若不想分桶，至少按 `hash(压缩后的 A)` 去重，相同轮廓只留 f 最大的一份。

---

## 参数与终止

```
k         : 每层保留状态数，先 200～2000，不够再 iterative widening
pos_keep  : 每件每方向保留落点数，1～3
λ         : 新死空间在位置分中的权重，~1（与体积同单位）
μ         : 段数增加的惩罚，远小于一件的体积，例如 0.01 * (H*W/平均段面积)
γ         : 大件罚，0.5～1

终止:
    某层 cand 为空
    或 所有剩余件在所有 beam 状态上都无落点
    或 已达到时限，返回 best
```

Iterative widening：同一套展开，\(k=k_0, 2k_0, 4k_0,\ldots\) 直到时限。不要靠把估价做成可采纳 A\* 来换质量。

---

## 明确不做的事

```
// 不要
open ← 全局优先队列按 f=g+h_可采纳 弹出     // 面积界太松，等于铺状态
u.value ← Σ (vol - hole)                    // 空穴会计不守恒，且偏好填缝小件
对 y = 0..H-1 全部尝试放置                 // 用角点
用长 H 的裸数组当 DP key                   // 用分段 skyline
把不同 |S| 的状态放一起取 top-k            // 必须分层
```

---

## 一次转移的数据流

```
u = (A, S, g)
        │
        ├─ 剩余件 i、方向 (h,w)
        ├─ CandidateSlots → 少数角点
        ├─ Place → A'
        └─ pos_score = vol(i) - λ Δdead - μ Δfrag
                │
                ▼
        v = (A', S∪{i}, g+vol(i))
                │
                ├─ 同 S 支配剪枝
                ├─ f = g + H_upper - γ large_pen
                └─ 分桶后进入下一层 beam
```
